apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: research-platform
  labels:
    app.kubernetes.io/name: research-platform
data:
  JWT_ALGORITHM: "HS256"
  AWS_REGION: "us-east-1"
  AWS_BUCKET_NAME: "research-marketplace"
  ENVIRONMENT: "production"
  MAX_UPLOAD_MB: "50"
  RATE_LIMIT_PER_MINUTE: "100"
